#include <stdio.h>

int main(int argc, char **argv){
    int num;
        printf("\nInsira um numero inteiro para descobrir se ele e par ou impar: ");
        scanf("%d", &num);
            funcParImpar(num);
    return 0;
}